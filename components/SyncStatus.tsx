'use client';

import React from 'react';
import SyncStatusAdvanced from '@/components/SyncStatusAdvanced';

const SyncStatus: React.FC = () => {
  return <SyncStatusAdvanced />;
};

export default SyncStatus; 